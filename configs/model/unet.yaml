# UNet model configuration for diffusion training
sample_size: 32
in_channels: 1
out_channels: 1
layers_per_block: 1
block_out_channels: [16, 32]
down_block_types: ["DownBlock2D", "DownBlock2D"]
up_block_types: ["UpBlock2D", "UpBlock2D"]

